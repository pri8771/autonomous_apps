# Gemini Instructions

Read `AGENTS.md` first and follow the App Factory context path. Use `.factory/repository-map.json` to locate the smallest authoritative context set. Follow the `dev → qa → main` promotion flow and do not mark work complete without required evidence.
